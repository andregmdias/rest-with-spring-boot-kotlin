version: '3.8'
services:
  postgres:
    container_name: rest_with_spring_boot_kt_db
    image: postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=rest_with_spring_boot_kt_db
